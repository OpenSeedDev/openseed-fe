import type { ReactNode } from 'react'
import { Check, ShieldCheck, Sparkles } from 'lucide-react'
import { Link } from 'react-router-dom'
export function AuthShell({title,description,children,footer,kicker='WELCOME TO OPENSEED'}:{title:string;description:string;children:ReactNode;footer?:ReactNode;kicker?:string}){return <div className="auth auth-v2"><div className="auth-layout"><aside className="auth-intro"><Link className="brand auth-brand" to="/"><span className="brand-mark">O</span><span><b>OpenSeed</b><small>Ideas grow together</small></span></Link><div className="auth-intro-copy"><span><Sparkles/>OPEN INNOVATION</span><h2>아이디어의 가능성을<br/>함께 발견하고 검증하세요.</h2><p>생각을 구체화하는 순간부터 기업과 연결되는 과정까지, OpenSeed가 성장의 흐름을 이어줍니다.</p><ul><li><Check/>AI를 활용한 아이디어 구체화</li><li><Check/>커뮤니티 기반 가능성 검증</li><li><Check/>인증 기업과의 안전한 연결</li></ul></div><div className="auth-trust"><ShieldCheck/><span><b>안전한 참여 환경</b><small>프로필과 기업 인증을 기반으로 신뢰도 높은 연결을 지원합니다.</small></span></div></aside><section className="auth-card"><span className="auth-kicker">{kicker}</span><h1>{title}</h1><p className="auth-description">{description}</p>{children}{footer&&<div className="auth-footer">{footer}</div>}</section></div></div>}
export function Field({label,error,children,hint}:{label:string;error?:string;children:ReactNode;hint?:string}){return <label className="form-field"><span>{label}</span>{children}{hint&&!error&&<small>{hint}</small>}{error&&<small className="field-error" role="alert">{error}</small>}</label>}
